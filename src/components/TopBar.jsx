import { Bell, ChevronLeft, Search } from "lucide-react";
import { useNavigate } from "react-router-dom";
export default function TopBar({title,back=false,action="notifications"}){const navigate=useNavigate();return <header className="flex items-center justify-between px-5 pb-4 pt-5"><div className="flex min-w-0 items-center gap-3">{back&&<button onClick={()=>navigate(-1)} className="rounded-xl p-2 text-slate-600 hover:bg-white"><ChevronLeft size={21}/></button>}<h1 className="truncate text-xl font-bold tracking-tight text-slate-900">{title}</h1></div>{action!=="none"&&<button onClick={()=>action==="notifications"&&navigate("/notifications")} className="relative rounded-xl p-2.5 text-slate-500 hover:bg-white">{action==="search"?<Search size={19}/>:<><Bell size={19}/><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-rose-500"/></>}</button>}</header>}