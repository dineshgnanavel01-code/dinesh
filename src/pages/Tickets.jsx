import {Filter,Plus,Search} from "lucide-react";import {useState} from "react";import {useNavigate} from "react-router-dom";import TopBar from "../components/TopBar";import Badge from "../components/Badge";import SectionHeader from "../components/SectionHeader";import {tickets} from "../data/mockData";
export default function Tickets(){const[query,setQuery]=useState("");const navigate=useNavigate();const filtered=tickets.filter(t=>`${t.title} ${t.id} ${t.requester}`.toLowerCase().includes(query.toLowerCase()));return <div className="animate-float-in"><TopBar title="Tickets"/><div className="px-5 pb-6"><div className="flex gap-2"><div className="relative flex-1"><Search size={17} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search tickets..." className="w-full rounded-2xl border border-slate-200 bg-white py-3 pl-10 pr-4 text-xs outline-none focus:border-teal-400"/></div><button className="grid w-12 place-items-center rounded-2xl border border-slate-200 bg-white text-slate-500"><Filter size={18}/></button></div><div className="mt-6"><SectionHeader title={`${filtered.length} tickets`} link="All status"/></div><div className="space-y-3">{filtered.map(t=><button key={t.id} onClick={()=>navigate(`/tickets/${t.id}`)} className="w-full rounded-2xl border border-slate-100 bg-white p-4 text-left shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold text-teal-700">{t.id}</p><h3 className="mt-1 text-sm font-bold text-slate-900">{t.title}</h3></div><Badge>{t.status}</Badge></div><div className="mt-4 flex items-center justify-between"><span className="text-[11px] text-slate-400">{t.requester} · {t.category}</span><Badge>{t.priority}</Badge></div><p className="mt-2 text-[10px] text-slate-400">{t.time}</p></button>)}</div><button className="fixed bottom-24 right-5 grid h-14 w-14 place-items-center rounded-2xl bg-teal-700 text-white shadow-xl"><Plus/></button></div></div>}