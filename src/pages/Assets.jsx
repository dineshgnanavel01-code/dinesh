import {HardDrive,Laptop,MoreVertical,Router,Search,Smartphone} from "lucide-react";import {useState} from "react";import TopBar from "../components/TopBar";import Badge from "../components/Badge";import SectionHeader from "../components/SectionHeader";import {assets} from "../data/mockData";
const icons={laptop:Laptop,phone:Smartphone,router:Router};export default function Assets(){const[query,setQuery]=useState("");const filtered=assets.filter(a=>`${a.name} ${a.id} ${a.user}`.toLowerCase().includes(query.toLowerCase()));return <div className="animate-float-in"><TopBar title="Assets"/><div className="px-5 pb-6"><div className="relative"><Search size={17} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search assets..." className="w-full rounded-2xl border border-slate-200 bg-white py-3 pl-10 pr-4 text-xs outline-none focus:border-teal-400"/></div><div className="mt-6"><SectionHeader title="Asset inventory" link="186 total"/></div><div className="space-y-3">{filtered.map(asset=>{const Icon=icons[asset.icon]||HardDrive;return <div key={asset.id} className="flex items-center gap-3 rounded-2xl border border-slate-100 bg-white p-4 shadow-sm transition hover:shadow-md"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-slate-100 text-slate-600"><Icon size={20}/></div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h3 className="truncate text-xs font-bold text-slate-800">{asset.name}</h3><Badge>{asset.status}</Badge></div><p className="mt-1 text-[10px] text-slate-400">{asset.id} · {asset.type}</p><p className="mt-1 text-[10px] font-medium text-slate-500">{asset.user}</p></div><button className="text-slate-400"><MoreVertical size={18}/></button></div>})}</div></div></div>}