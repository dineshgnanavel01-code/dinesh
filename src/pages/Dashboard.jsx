import { ArrowUpRight, CheckCircle2, Clock3, Ticket, Users } from "lucide-react";
import { useNavigate } from "react-router-dom";
import TopBar from "../components/TopBar";import StatCard from "../components/StatCard";import SectionHeader from "../components/SectionHeader";import {activities,stats} from "../data/mockData";
const icons={ticket:Ticket,laptop:Users,check:CheckCircle2,bell:Clock3};
export default function Dashboard(){const navigate=useNavigate();return <div className="animate-float-in"><TopBar title="Good morning, Alex"/><div className="px-5 pb-6"><div className="rounded-3xl bg-gradient-to-br from-teal-800 to-teal-600 p-5 text-white shadow-lg"><p className="text-xs text-teal-100">Thursday, August 27</p><div className="mt-3 flex items-end justify-between"><div><h2 className="text-2xl font-bold tracking-tight">IT operations are healthy</h2><p className="mt-1 text-xs text-teal-100">4 items need your attention today.</p></div><div className="grid h-12 w-12 place-items-center rounded-2xl bg-white/10"><ArrowUpRight/></div></div></div><div className="mt-6"><SectionHeader title="Overview" link="View reports"/></div><div className="grid grid-cols-2 gap-3">{stats.map(item=><StatCard key={item.label} item={item}/>)}</div><div className="mt-7"><SectionHeader title="Recent activity" link="See all" onClick={()=>navigate("/notifications")}/></div><div className="overflow-hidden rounded-2xl border border-slate-100 bg-white">{activities.map((a,i)=>{const Icon=icons[a.icon]||Clock3;return <div key={a.title} className={`flex gap-3 p-4 ${i?"border-t border-slate-100":""}`}><div className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-teal-50 text-teal-700"><Icon size={17}/></div><div className="min-w-0 flex-1"><p className="text-xs font-semibold text-slate-800">{a.title}</p><p className="mt-1 truncate text-[11px] text-slate-400">{a.detail}</p></div><span className="whitespace-nowrap text-[10px] text-slate-400">{a.time}</span></div>})}</div><div className="mt-7"><SectionHeader title="Quick actions"/></div><div className="grid grid-cols-3 gap-3">{[["Tickets",Ticket,"/tickets"],["Tasks",CheckCircle2,"/tasks"],["Assets",Users,"/assets"]].map(([label,Icon,to])=><button key={label} onClick={()=>navigate(to)} className="rounded-2xl border border-slate-100 bg-white p-4 text-left shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><Icon size={19} className="text-teal-700"/><p className="mt-3 text-xs font-bold text-slate-800">{label}</p></button>)}</div></div></div>}